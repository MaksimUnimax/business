# business

Repository structure:

- `app/` — application code, parsers, tests, data pipelines.
- `docs/` — project documentation, rules, methodology, reports.

Current imported app:

- `app/source-ingestion/` — source ingestion probe for classifier/source-backed catalog work.

Chat workflow rules:

- `docs/rules/chatgpt_workflow_rules.md`
